import { Student } from './../../entities/students/student.entity';

export class Secondary extends Student {
 /* getScore(): any {
    let a:string="";
    if (this.getPercentage() < 60) {
      a= "B";
    }
    if (this.getPercentage() > 60) {
      a= "A";
    }
    if (this.getPercentage() > 75) {
      a= "A+";
    }
    console.log("score secondary : "+a);
    return a;
  }*/
}